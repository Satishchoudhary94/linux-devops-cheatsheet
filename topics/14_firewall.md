```markdown
# 🔥 UFW (Uncomplicated Firewall) Commands

- `ufw status` – Show current firewall status
- `ufw enable` – Enable the firewall
- `ufw disable` – Disable the firewall
- `ufw allow 22` – Allow SSH
- `ufw allow 80` – Allow HTTP
- `ufw allow 443` – Allow HTTPS
- `ufw deny 23` – Deny Telnet
- `ufw delete allow 80` – Remove HTTP allow rule
```
